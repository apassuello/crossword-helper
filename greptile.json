{
  "strictness": 1,
  "commentTypes": ["logic", "syntax", "style"],
  "fileChangeLimit": 1000,
  "includeConfidenceScore": true,
  "instructions": "Treat every line below as newly written code being submitted for review for the first time. Scope is cli/** (the Python CLI — the single source of truth for crossword logic: autofill/CSP, beam search, iterative repair, hybrid fill, pattern matching, pause/resume state serialization), src/** (the React GUI), and backend/** (the thin HTTP wrapper around the CLI, including the CLI-adapter seam and its routes — a known defect hotspot where past bugs have shipped). Hunt for real bugs: logic errors, off-by-one and edge-case mishandling, race conditions, incorrect state/lock handling, unhandled exceptions, resource leaks, and any function whose behavior contradicts its own docstring or nearby comments. Be exhaustive rather than concise."
}
